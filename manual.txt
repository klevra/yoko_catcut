═══════════════════════════════════════════════════════════════════════════════
                        YOKO CATCUT v0.1.1
                    AI-Powered Video Editing Platform
                            사용자 매뉴얼
═══════════════════════════════════════════════════════════════════════════════

📖 목차
═══════════════════════════════════════════════════════════════════════════════

1. 시스템 요구사항
2. 설치
3. 실행 방법
4. 중지 방법
5. 웹 인터페이스 사용법
6. 문제 해결
7. 고급 설정

═══════════════════════════════════════════════════════════════════════════════
1. 시스템 요구사항
═══════════════════════════════════════════════════════════════════════════════

운영체제:
  ✓ macOS 10.15+
  ✓ Windows 10+ (WSL2 권장)
  ✓ Linux (Ubuntu 20.04+)

필수 소프트웨어:
  ✓ Docker Desktop (4.0+)
  ✓ Git
  ✓ 웹 브라우저 (Chrome, Edge, Safari)

최소 사양:
  ✓ CPU: 2 cores 이상
  ✓ RAM: 4GB 이상
  ✓ Disk: 20GB 이상

═══════════════════════════════════════════════════════════════════════════════
2. 설치
═══════════════════════════════════════════════════════════════════════════════

2.1 Docker Desktop 설치
─────────────────────────────────────────────────────────────────────────────

macOS:
  1. Docker 공식 웹사이트 방문: https://www.docker.com/products/docker-desktop
  2. "Download for Mac" 클릭
  3. 다운로드된 DMG 파일 실행
  4. Applications 폴더로 드래그 앤 드롭
  5. Applications 폴더에서 Docker.app 실행

Windows:
  1. Docker Desktop for Windows 다운로드
  2. 설치 마법사 따라 진행
  3. WSL2 백엔드 사용 권장

Linux:
  ```bash
  curl -fsSL https://get.docker.com -o get-docker.sh
  sudo sh get-docker.sh
  ```

2.2 Yoko CatCut 저장소 클론
─────────────────────────────────────────────────────────────────────────────

```bash
cd ~/Documents
git clone https://github.com/klevra/yoko_catcut.git
cd yoko_catcut
```

═══════════════════════════════════════════════════════════════════════════════
3. 실행 방법
═══════════════════════════════════════════════════════════════════════════════

3.1 GUI를 통한 실행 (권장)
─────────────────────────────────────────────────────────────────────────────

macOS:
  1. Finder에서 yoko_catcut 폴더로 이동
  2. start.command 파일 더블클릭
  3. "이 개발자가 확인되지 않았습니다" 메시지 나타나면:
     - 우클릭 → 열기 선택
     - 또는 보안 설정에서 허용 클릭
  4. 자동으로 브라우저가 열리고 http://localhost:3000 접속
  5. 앱 준비 완료 (약 30-60초 소요)

Windows (PowerShell):
  1. yoko_catcut 폴더에서 우클릭
  2. "PowerShell 여기서 열기" 선택
  3. 다음 명령 실행:
     ```powershell
     .\start.command
     ```

3.2 터미널을 통한 실행
─────────────────────────────────────────────────────────────────────────────

```bash
cd ~/Documents/yoko_catcut
docker compose up --build
```

실행 순서:
  1. 🐳 Docker 이미지 빌드 (처음 실행할 때만, 5-10분 소요)
  2. 🚀 컨테이너 시작
  3. 📡 백엔드 서버 초기화
  4. 🎨 프론트엔드 서버 시작

3.3 실행 후 확인
─────────────────────────────────────────────────────────────────────────────

실행이 완료되면 다음 페이지에 접속 가능:

  🌐 프론트엔드 (Web UI):
     http://localhost:3000

  🔌 백엔드 API:
     http://localhost:8000

  📚 API 문서 (Swagger):
     http://localhost:8000/docs

  📖 API 문서 (ReDoc):
     http://localhost:8000/redoc

═══════════════════════════════════════════════════════════════════════════════
4. 중지 방법
═══════════════════════════════════════════════════════════════════════════════

4.1 GUI를 통한 중지 (권장)
─────────────────────────────────────────────────────────────────────────────

macOS:
  1. Finder에서 yoko_catcut 폴더 이동
  2. stop.command 파일 더블클릭
  3. 모든 컨테이너 자동 중지

4.2 터미널을 통한 중지
─────────────────────────────────────────────────────────────────────────────

```bash
cd ~/Documents/yoko_catcut
docker compose down
```

또는 Ctrl+C (터미널에서 실행 중인 경우)

4.3 강제 중지
─────────────────────────────────────────────────────────────────────────────

모든 컨테이너 강제 중지:
```bash
docker compose down -v
```

포트 충돌 해결 (같은 포트 사용 중인 경우):
```bash
# macOS/Linux
lsof -i :3000          # 포트 3000 사용 프로세스 확인
kill -9 <PID>          # 프로세스 강제 종료

# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

═══════════════════════════════════════════════════════════════════════════════
5. 웹 인터페이스 사용법
═══════════════════════════════════════════════════════════════════════════════

5.1 대시보드 구성
─────────────────────────────────────────────────────────────────────────────

┌─────────────────────────────────────────────────────────────────┐
│                    YOKO CATCUT 대시보드                          │
├─────────────────────────────────────────────────────────────────┤
│  ▼ 헤더                                                          │
│  Yoko CatCut - AI-powered video editing platform                │
├─────────────────────────────────────────────────────────────────┤
│  ▼ 메인 영역                                                     │
│  ┌──────────────────┐  ┌────────────────────────┐               │
│  │  새 프로젝트 생성  │  │  프로젝트 목록          │               │
│  │  ┌────────────┐  │  │  ┌──────────────────┐  │               │
│  │  │ 프로젝트명  │  │  │  │ 프로젝트 카드     │  │               │
│  │  │ [입력 필드]  │  │  │  │ - 생성 날짜      │  │               │
│  │  │            │  │  │  │ - 경로           │  │               │
│  │  │[생성 버튼]   │  │  │  │ [열기] [삭제]    │  │               │
│  │  └────────────┘  │  │  └──────────────────┘  │               │
│  └──────────────────┘  └────────────────────────┘               │
└─────────────────────────────────────────────────────────────────┘

5.2 프로젝트 생성
─────────────────────────────────────────────────────────────────────────────

1. 왼쪽 패널의 "New Project" 섹션 찾기
2. "Project name" 입력 필드에 프로젝트 이름 입력
   예: "여름휴가 영상", "유튜브_숏폼_001"
3. "Create Project" 버튼 클릭
4. 프로젝트가 오른쪽 목록에 추가됨

프로젝트명 규칙:
  ✓ 영문, 숫자, 언더스코어(_), 하이픈(-) 사용 가능
  ✗ 공백, 특수문자는 사용 불가

5.3 프로젝트 관리
─────────────────────────────────────────────────────────────────────────────

각 프로젝트 카드에서:

  🔓 [열기] 버튼:
     - 프로젝트 상세 페이지로 이동 (v0.2.0 지원 예정)
     - 파일 업로드, 작업 관리 등

  🗑️ [삭제] 버튼:
     - 프로젝트 완전 삭제 (되돌릴 수 없음)
     - 삭제 전 확인 메시지 표시

5.4 파일 업로드 (v0.2.0)
─────────────────────────────────────────────────────────────────────────────

향후 업데이트에서 제공:
  • 드래그 & 드롭 업로드
  • 다중 파일 업로드
  • 진행 상황 표시

지원 포맷:
  ✓ MP4
  ✓ MOV
  ✓ MKV
  ✓ WebM

최대 파일 크기: 10GB

═══════════════════════════════════════════════════════════════════════════════
6. 문제 해결
═══════════════════════════════════════════════════════════════════════════════

6.1 "Docker not running" 에러
─────────────────────────────────────────────────────────────────────────────

증상: start.command 실행 시 에러 발생

해결책:
  1. Docker Desktop 애플리케이션 시작
  2. Docker 아이콘이 메뉴바에 표시될 때까지 대기
  3. start.command 다시 실행

6.2 포트 충돌 (Port already in use)
─────────────────────────────────────────────────────────────────────────────

증상: "Port 3000 is already allocated"

해결책:

  방법 1: 기존 프로세스 종료
  ```bash
  docker ps                    # 실행 중인 컨테이너 확인
  docker stop <container_id>   # 컨테이너 중지
  ```

  방법 2: application.yml에서 포트 변경
  ```yaml
  app:
    server:
      backend_port: 8001       # 8000 → 8001 변경
      frontend_port: 3001      # 3000 → 3001 변경
  ```

6.3 브라우저가 자동으로 열리지 않음
─────────────────────────────────────────────────────────────────────────────

해결책:
  1. 수동으로 브라우저 열기
  2. 주소창에 http://localhost:3000 입력
  3. Enter 키 누르기

6.4 느린 초기 로딩
─────────────────────────────────────────────────────────────────────────────

원인: Docker 이미지 빌드 중

대기 시간:
  • 첫 실행: 5-10분 (이미지 다운로드 및 빌드)
  • 이후 실행: 10-30초 (캐시된 이미지 사용)

진행 상황 확인:
  ```bash
  docker compose logs -f backend
  ```

6.5 메모리 부족 에러
─────────────────────────────────────────────────────────────────────────────

증상: "Out of memory" 에러

해결책:
  1. Docker Desktop 설정 열기
  2. Preferences → Resources
  3. Memory 값을 최소 4GB로 설정
  4. Docker 재시작

═══════════════════════════════════════════════════════════════════════════════
7. 고급 설정
═══════════════════════════════════════════════════════════════════════════════

7.1 application.yml 설정
─────────────────────────────────────────────────────────────────────────────

파일 경로: ~/Documents/yoko_catcut/application.yml

주요 설정 항목:

```yaml
app:
  server:
    host: 0.0.0.0              # 서버 바인드 주소
    backend_port: 8000         # 백엔드 포트
    frontend_port: 3000        # 프론트엔드 포트

  workspace:
    root_path: ./workspace     # 프로젝트 저장 위치

  storage:
    type: local                # 로컬 또는 nas

  upload:
    max_file_size_gb: 10       # 최대 파일 크기

  model:
    whisper_model: large-v3    # Whisper 모델 (v0.3.0)
    device: cpu                # cpu 또는 cuda
```

7.2 환경별 설정 파일
─────────────────────────────────────────────────────────────────────────────

기본 설정:
  application.yml             (기본)
  application-local.yml       (로컬 개발)
  application-dev.yml         (개발 환경)
  application-prod.yml        (프로덕션)

활성화 방법:
```bash
export YOKO_CONFIG=application-dev.yml
docker compose up
```

7.3 로그 확인
─────────────────────────────────────────────────────────────────────────────

실시간 로그 보기:
```bash
docker compose logs -f backend    # 백엔드 로그
docker compose logs -f frontend   # 프론트엔드 로그
docker compose logs -f            # 모든 로그
```

로그 파일 위치:
  ./logs/                         # 저장된 로그 파일

7.4 데이터 초기화
─────────────────────────────────────────────────────────────────────────────

모든 프로젝트 및 데이터 삭제:
```bash
docker compose down -v            # 볼륨 포함 삭제
rm -rf workspace/*                # 작업 폴더 초기화
rm -rf data/*                     # 데이터 폴더 초기화
```

주의: 이 작업은 복구할 수 없습니다!

7.5 개발 모드 실행
─────────────────────────────────────────────────────────────────────────────

소스코드 변경사항 자동 반영:

```bash
# 개별 실행
cd backend && pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

cd frontend && npm install
npm run dev
```

═══════════════════════════════════════════════════════════════════════════════
8. 자주 묻는 질문 (FAQ)
═══════════════════════════════════════════════════════════════════════════════

Q: 클라우드 스토리지 지원하나요?
A: v1.0.0에서 지원 예정입니다. 현재는 로컬/NAS만 지원합니다.

Q: 여러 사용자 동시 사용 가능한가요?
A: v0.1.1에서는 단일 사용자 기준으로 설계했습니다.
   v1.0.0에서 다중 사용자 지원 예정입니다.

Q: YouTube에서 직접 비디오 다운로드 가능한가요?
A: v0.7.0에서 지원 예정입니다. 현재는 로컬 업로드만 가능합니다.

Q: 자막 자동 생성 기능은 언제 나오나요?
A: v0.3.0에서 제공 예정입니다 (2026년 6월 말 예상).

Q: 썬네일/숏폼 생성은?
A: v0.5.0-v0.6.0에서 단계적으로 지원할 예정입니다.

Q: 데이터는 어디에 저장되나요?
A: ./workspace/ 디렉토리에 저장됩니다. 필요시 다른 위치로 변경 가능합니다.

═══════════════════════════════════════════════════════════════════════════════
9. 지원 및 문제 보고
═══════════════════════════════════════════════════════════════════════════════

버그 보고:
  Repository: https://github.com/klevra/yoko_catcut/issues
  1. "New Issue" 클릭
  2. 문제 상세 설명
  3. 스크린샷 첨부 (옵션)

문서:
  - Requirements: docs/yoko_catcut_requirements.md
  - Roadmap: docs/roadmap.md
  - Changelog: docs/changelog.md

═══════════════════════════════════════════════════════════════════════════════
10. 버전 정보
═══════════════════════════════════════════════════════════════════════════════

현재 버전: v0.1.1
릴리스 날짜: 2026-06-03
상태: STABLE

주요 기능:
  ✓ 프로젝트 관리
  ✓ Workspace 관리
  ✓ 파일 업로드 (v0.2.0에서 활성화)

다음 버전: v0.2.0 (예상: 2026-06-30)
  • 파일 검증
  • 메타데이터 분석

═══════════════════════════════════════════════════════════════════════════════

이 문서는 Yoko CatCut v0.1.1을 기준으로 작성되었습니다.
최신 정보는 README.md와 공식 GitHub 저장소를 참조하세요.

마지막 수정: 2026-06-03
